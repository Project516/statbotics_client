# Changelog

## 0.1.0

- Initial release: `StatboticsClient` (`getEvent`, `getEventTeams`,
  `getTeamYear`, `getEventMatches`, `getEventTeamsBasic`) with typed EPA,
  event, team, and match models, plus transient-error retry with backoff.
